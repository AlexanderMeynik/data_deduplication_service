name: Build and Test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15.8
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: password
          POSTGRES_DB: deduplication
        ports:
          - "5501:5432"
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y clang ninja-build libpq-dev

      - name: Build and install glog
        run: |
          git clone https://github.com/google/glog.git
          cd glog
          mkdir build
          cd build
          cmake -G Ninja ..
          cmake --build .
          sudo ninja install
          cd ../..

      - name: Build and install libpqxx
        run: |
          git clone https://github.com/jtv/libpqxx.git
          cd libpqxx
          sudo apt install -y automake
          ./configure --enable-maintainer-mode --enable-audit CXXFLAGS=-O3
          make -j$(nproc)
          sudo make install
          cd ../..

      - name: Build and install googletest
        run: |
          git clone https://github.com/google/googletest
          cd googletest
          mkdir build
          cd build
          cmake -G Ninja ..
          cmake --build .
          sudo ninja install
          cd ../..

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and test your project
        run: |
          mkdir build && cd build
          cmake -G Ninja ..
          ninja
          cd test
          ctest --output-on-failure
